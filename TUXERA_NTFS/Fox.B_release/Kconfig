config TNTFS_FS
	tristate "TNTFS file system support"
	default m
	select NLS
	help
	  NTFS is the file system of Microsoft Windows NT and later.

	  To compile this file system support as a module, choose M here: the
	  module will be called ntfs.

